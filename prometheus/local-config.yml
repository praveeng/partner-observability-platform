global:
  scrape_interval: 30s
  scrape_timeout: 10s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/partner-recording-rules.yml

# Alloy owns application scraping. Prometheus receives only post-relabel remote write.
scrape_configs: []
