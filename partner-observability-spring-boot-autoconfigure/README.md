# Spring Boot auto-configuration module

Future home of conditional Spring Boot 2.7 configuration, request/client interception, lifecycle management, and validated configuration properties. It depends on core and must preserve the asynchronous business-path boundary.

M0 intentionally contains no Java sources.
