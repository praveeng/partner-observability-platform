# Security tests

Adversarial disclosure and partner-isolation suites will live here. They must inspect pre-queue values and every sink, cover malformed/error paths, and prove prohibited values are absent rather than merely asserting sanitizer calls.
