server:
  address: 127.0.0.1

spring:
  application:
    name: partner-observability-test-app
  main:
    banner-mode: "off"

logging:
  level:
    root: WARN
    com.partner.observability.testapp: INFO
