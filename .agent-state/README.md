# Agent state

`status.json` is the machine-readable handoff for fresh autonomous agents. Keep it valid JSON; use only the declared lifecycle states; update it whenever the active milestone or lifecycle changes. Narrative plans and acceptance evidence belong in `PLANS.md` and `docs/`, not in invented state fields.
