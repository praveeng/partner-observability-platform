# Terraform examples

Non-production examples for approved modules will live here. Examples must be safe by default, use placeholders rather than credentials, and never apply automatically. M0 intentionally contains no Terraform configuration.
