# Core SDK module

Future home of framework-independent telemetry models, payload safety policy, immutable safe events, bounded buffering, non-blocking drop behavior, failure containment, and asynchronous exporter interfaces.

This module must not depend on Spring. M0 intentionally contains no Java sources.
