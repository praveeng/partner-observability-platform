interface:
  display_name: "Terraform ECS Review"
  short_description: "Review Terraform and ECS deployment safety"
  default_prompt: "Use $terraform-ecs-review to review Terraform and ECS changes without deploying."
