# Synthetic fixtures

Only unmistakably synthetic data is permitted. Future fixtures should include secret-shaped sentinels, identifiers, nested/malformed values, Base64-like inputs, and binary/document markers designed to prove removal, masking, and exclusion. Never copy production or customer-derived payloads.
